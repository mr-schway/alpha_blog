<h1 class="text-center mt-4"><%= @user.username %></h1> 

<%= gravatar_for @user, size: 200 %> 

<div class="text-center mt-4"> 
  <%= link_to "Edit profile", edit_user_path(@user), class: "btn btn-outline-secondary" %> 
</div>

<h3 class="text-center mt-4">Articles</h3> 

<div class="flickr_pagination">
  <%= will_paginate @articles, :container => false %>
</div> 

<%= render 'articles/article' %> 

<div class="flickr_pagination mb-5">
  <%= will_paginate @articles, :container => false %>
</div> 

<div class="d-flex justify-content-center">
     <%= link_to 'Return to list of Bloggers', users_path, class: "mb-5 btn btn-success btn-lg btn-block" %> 
</div>