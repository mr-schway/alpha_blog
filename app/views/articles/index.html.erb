<h1 class="text-center mt-4">List of all articles</h1>

<div class="flickr_pagination">
  <%= will_paginate @artilces, :container => false %>
</div> 

<%= render 'article' %> 

<div class="flickr_pagination mb-5">
  <%= will_paginate @artilces, :container => false %>
</div>